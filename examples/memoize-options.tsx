// const handleFavorite = React.useCallback(() => {
//   void callSomeApi(!favorite);
// }, [favorite]);

// const handleFavorite = React.useCallback((favorite: boolean) => {
//   void callSomeApi(!favorite);
// }, []);

// // callback is defined in parent
// const callbackRef = React.useRef(callback);
// callbackRef.current = callback;

// const handleFavorite = React.useCallback((data: any) => {
//   void callSomeApi(callbackRef.current(data));
// }, []);
